演示项目：这个文件夹是给 clone 模板的人看效果用的示例素材。

它演示了三件事：
1. 素材按「角色 / 项目」两级放：public/alice/demo-project/
2. 子目录可以随便建、层级不限：refs/ 里放着参考图
3. 文本类文件（.txt / .md）会以纯文本 / 排版后的样子在页面里打开

真实使用时，把 alice 换成你的角色名、demo-project 换成项目名，
然后删掉这个示例文件夹即可（别忘了 materials-times.json 会在下次构建时自动清理）。
